<a id="<%= label.toLowerCase().replace(/\s+/g, '-') %>" href="<%= url %><% if (static) { %><% if (url !== "/") { %>.html<% } %><% } %>" class=" btn btn-md btn-secondary" <i class=" icon-name"></i>
  <%= (label == "Committee") ? "Committees" : label %>
</a>