<!-- level can be h1 - h6 -->
<%
const headingLevel = level || "h3";
const headingTag = String(headingLevel).replace(/^h/, "");
%>
<h<%= headingTag %> class="text-center text-<%= headingLevel %> font-bold text-secondary font-montserrat mb-4"><%- text %></h<%= headingTag %>>
